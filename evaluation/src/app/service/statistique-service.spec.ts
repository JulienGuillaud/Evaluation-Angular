import { StatistiqueService } from './statistique-service';

describe('StatistiqueService', () => {
  it('should create an instance', () => {
    expect(new StatistiqueService()).toBeTruthy();
  });
});
